<Project Sdk="Microsoft.NET.Sdk.BlazorWebAssembly">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <NoDefaultLaunchSettingsFile>true</NoDefaultLaunchSettingsFile>
    <StaticWebAssetProjectMode>Default</StaticWebAssetProjectMode>
    <RootNamespace>ASCII_Hero.Client</RootNamespace>
    <AssemblyName>$(AssemblyName.Replace(' ', '_'))</AssemblyName>
    <BlazorDisableThrowNavigationException>true</BlazorDisableThrowNavigationException>
    <!--
      Ahead-of-time compiles the app to native WebAssembly instead of running
      as interpreted IL. Significantly improves runtime performance (frame
      rate) for CPU-bound code like the game loop, at the cost of larger
      publish output and longer publish time. Only affects `dotnet publish`,
      not local `dotnet run`/F5 debugging.
    -->
    <RunAOTCompilation>true</RunAOTCompilation>
    <!--
      Set to true only by the GitHub Pages publish workflow (see
      .github/workflows/deploy-gh-pages.yml), which publishes this project
      standalone rather than hosted inside the ASCII Hero server project.
      Gates the RootComponents registration in Program.cs - see comment there.
    -->
    <DefineConstants Condition="'$(StandaloneBuild)' == 'true'">$(DefineConstants);STANDALONE_BUILD</DefineConstants>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly" Version="10.0.11" />
  </ItemGroup>

</Project>
