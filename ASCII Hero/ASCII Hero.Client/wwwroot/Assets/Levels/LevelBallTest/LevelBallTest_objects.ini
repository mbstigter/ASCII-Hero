[ObjectCodes]
0 = Player
1 = BouncingBall
2 = WallHorizontal
3 = WallVertical
4 = TestCollectable
5 = ToxicPlant

[Player]
Asset = Player
Clip = idle
Kind = Player

[WallHorizontal]
Asset = SteelPlatform
Clip = default
Kind = StaticObject
; Authored as a 1-wide tileable unit (TileAxis = Horizontal); Repeat spans the
; full 80-cell width of the enclosed test box for the top/bottom walls.
Repeat = 80

[WallVertical]
Asset = BrickWall
Clip = default
Kind = StaticObject
; Authored as a 1-tall tileable unit (TileAxis = Vertical); Repeat spans the
; area between the top/bottom walls for the left/right walls.
Repeat = 22

[BouncingBall]
Asset = Ball
Clip = default
Kind = DynamicObject
; Same "weightless, perfectly elastic" placeholder tuning as Level1's ball -
; isolated here (no other platforms, fully enclosed box) to observe whether it
; keeps bouncing forever or ends up settled in a corner.
GravityAffected = false
Restitution = 1.0
InitialVelocityX = 14
InitialVelocityY = 10
; The camera follows this ball instead of the (stationary, off to the side)
; player, so its full bounce path stays visible.
CameraTarget = true

[TestCollectable]
; Throwaway placement proving the generic Collectable removal plumbing works
; end-to-end: walk the player into it and it should disappear (queued for
; removal by CollisionSystem, applied via World2D.ApplyPendingRemovals).
Asset = Ring
Clip = default
Kind = Collectable

[ToxicPlant]
Asset = ToxicPlant
Clip = idle
Kind = StaticEnemy

